let ROWS=[], DEVICES=[], FILTERED=[];
function render(data){q('countLabel').textContent=`${data.length} thiết bị`; q('rows').innerHTML=data.length?data.map(r=>`<tr><td class="device-code">${r.device_code}</td><td>${r.device_name}</td><td class="code-only">${r.department_code||''}</td><td>${formatDateVN(r.rating_date)}</td><td>${r.age_score}</td><td>${r.performance_score}</td><td>${r.repair_score}</td><td>${r.inspection_score}</td><td>${r.sparepart_score}</td><td><strong>${r.total_score}</strong></td><td><span class="tag ${r.grade==='A'?'green':r.grade==='B'?'yellow':r.grade==='C'?'orange':'red'}">${r.grade}</span></td><td>${r.recommendation||''}</td><td><div class="table-actions"><button class="btn" onclick="editRow(${r.id})">Cập nhật</button><button class="btn btn-danger" onclick="delRow(${r.id})">Xóa</button></div></td></tr>`).join(''):'<tr><td colspan="13" class="center-empty">Chưa có dữ liệu.</td></tr>';}
function applyFilter(){const text=q('searchInput').value.toLowerCase(); const grade=q('gradeFilter').value; FILTERED=ROWS.filter(r=>(!text||[r.device_code,r.device_name,r.department_code].join(' ').toLowerCase().includes(text))&&(grade==='ALL'||r.grade===grade)); render(FILTERED);}
function resetForm(){q('form').reset(); q('ratingDate').value=new Date().toISOString().slice(0,10); q('ageScore').value=25; q('performanceScore').value=25; q('repairScore').value=20; q('inspectionScore').value=15; q('sparepartScore').value=15;}
function editRow(id){const r=ROWS.find(x=>x.id===id); if(!r)return; q('deviceId').value=r.device_id; q('ratingDate').value=r.rating_date||''; q('evaluator').value=r.evaluator||''; q('ageScore').value=r.age_score||0; q('performanceScore').value=r.performance_score||0; q('repairScore').value=r.repair_score||0; q('inspectionScore').value=r.inspection_score||0; q('sparepartScore').value=r.sparepart_score||0; q('recommendation').value=r.recommendation||''; q('note').value=r.note||''; q('form').scrollIntoView({behavior:'smooth'});}
async function delRow(id){if(!confirm('Xóa đánh giá này?'))return; await api(`/api/quality-ratings/${id}`,{method:'DELETE'}); await load();}
async function load(){DEVICES=await api('/api/devices'); ROWS=await api('/api/quality-ratings'); q('deviceId').innerHTML=DEVICES.map(d=>`<option value="${d.id}">${d.device_code} - ${d.name}</option>`).join(''); applyFilter();}
function exportExcel(){const rows=FILTERED.map(r=>({'Mã thiết bị':r.device_code,'Tên thiết bị':r.device_name,'Khoa':r.department_code,'Ngày đánh giá':r.rating_date,'Tổng điểm':r.total_score,'Cấp':r.grade,'Khuyến nghị':r.recommendation,'Ghi chú':r.note})); const ws=XLSX.utils.json_to_sheet(rows); const wb=XLSX.utils.book_new(); XLSX.utils.book_append_sheet(wb,ws,'PhanCap'); XLSX.writeFile(wb,'phan_cap_chat_luong.xlsx');}
document.addEventListener('DOMContentLoaded',async()=>{setLayout('quality','Phân cấp chất lượng','Chấm điểm A/B/C/D theo tuổi thiết bị, hiệu suất, sửa chữa, kiểm định và phụ tùng'); await load(); resetForm(); q('filterBtn').onclick=applyFilter; q('searchInput').oninput=applyFilter; q('gradeFilter').onchange=applyFilter; q('exportBtn').onclick=exportExcel; q('resetBtn').onclick=resetForm; q('form').onsubmit=async e=>{e.preventDefault(); const p={device_id:Number(q('deviceId').value),rating_date:q('ratingDate').value,evaluator:q('evaluator').value,age_score:Number(q('ageScore').value||0),performance_score:Number(q('performanceScore').value||0),repair_score:Number(q('repairScore').value||0),inspection_score:Number(q('inspectionScore').value||0),sparepart_score:Number(q('sparepartScore').value||0),recommendation:q('recommendation').value,note:q('note').value}; await api('/api/quality-ratings',{method:'POST',body:JSON.stringify(p)}); resetForm(); await load();};});
