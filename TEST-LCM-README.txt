TEST-LCM.cmd = bộ kiểm tra nhanh, chỉ đọc dữ liệu.

Cách dùng:
1. Khởi động phần mềm bằng START-QY4-TTBYT.cmd.
2. Double-click TEST-LCM.cmd.
3. Gửi ảnh màn hình kết quả PASS/WARN/FAIL nếu cần rà tiếp.

Bộ kiểm tra không tạo, sửa hoặc xóa dữ liệu nghiệp vụ.
